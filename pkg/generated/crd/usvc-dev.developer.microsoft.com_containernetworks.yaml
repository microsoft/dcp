---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: containernetworks.usvc-dev.developer.microsoft.com
spec:
  group: usvc-dev.developer.microsoft.com
  names:
    kind: ContainerNetwork
    listKind: ContainerNetworkList
    plural: containernetworks
    singular: containernetwork
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ContainerNetwork represents a network that can be consumed by
          Container instances Its lifetime is independent from the lifetime of containers
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ContainerNetworkSpec defines the desired state of a ContainerNetwork
            properties:
              ipv6:
                description: Shouild IPv6 be enabled for the network?
                type: boolean
              networkName:
                description: Name of the network (if omitted, a name is generated
                  based on the resource name)
                type: string
              persistent:
                description: Should this network be created and persisted between
                  DCP runs?
                type: boolean
            type: object
          status:
            description: ContainerNetworkStatus defines the current state of a ContainerNetwork
            properties:
              containerIds:
                description: The list of container IDs connected to the network
                items:
                  type: string
                type: array
              driver:
                description: The driver of the network
                type: string
              gateways:
                description: Gateways allocated to the network (if any)
                items:
                  type: string
                type: array
              id:
                description: The ID of the network
                type: string
              ipv6:
                description: Does the network support IPv6?
                type: boolean
              message:
                description: If the network is in a failed state, this is the reason
                type: string
              networkName:
                description: The name of the network
                type: string
              state:
                description: The current state of the network
                type: string
              subnets:
                description: Subnets allocated to the network (if any)
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
